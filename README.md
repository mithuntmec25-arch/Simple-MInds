# Simple-MInds
A Python CLI tool that scans Google Play search results, detects suspicious or fake apps using keyword + rating filters, extracts developer info, and auto-generates impersonation report emails. Simple, fast, and useful for basic app-fraud detection.
